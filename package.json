{
  "name": "stoneconnect",
  "version": "0.1.0",
  "description": "StoneConnect is a version of connect4 that is cooler",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf -f build $$ mkdir build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "blackeuler",
  "license": "MIT",
  "dependencies": {
    "rimraf": "^3.0.2"
  }
}
